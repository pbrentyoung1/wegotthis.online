{
  "name": "forworship-creative",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix docs-site run dev",
    "build": "npm --prefix docs-site run build",
    "preview": "npm --prefix docs-site run preview",
    "install:docs": "npm --prefix docs-site install"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
