<template>
  <BCard no-body>
    <BCardBody>
      <div>
        <Link href="" class="link-reset text-uppercase fw-semibold">
          {{ item.title }}
        </Link>

        <div class="py-2">
          <span class="fs-xl fw-bold me-2">{{ item.value }}%</span>
          <span class="fw-semibold text-muted fs-7">{{ toPascalCase(item.status) }}</span>
        </div>
      </div>

      <BProgress :value="item.value" bg-variant="success" class="progress-md bg-opacity-25" />

      <div class="mt-2 text-muted">
        {{ item.description }}
      </div>
    </BCardBody>
  </BCard>
</template>

<script setup lang="ts">
import { Link } from '@inertiajs/vue3'

import { BCard, BCardBody, BProgress } from 'bootstrap-vue-next'
import { toPascalCase } from '@/utils/helpers'
import type { Widget6Type } from './data'
defineProps<{ item: Widget6Type }>()
</script>
